# .env for Spring Boot application
#
# This file contains the environment variables needed for your Spring Boot application,
# specifically for configuring the PostgreSQL database connection and Hibernate/JPA settings.
# After editing this file, save it as `.env` (remove the `.template` suffix).

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------

# Database URL (e.g., jdbc:postgresql://localhost:5432/my_database)
# Replace <your-database-host>, <your-database-port>, and <your-database-name>
# with your actual database details.
SPRING_DATASOURCE_URL=jdbc:postgresql://<your-database-host>:<your-database-port>/<your-database-name>

# Database username (replace <your-database-username> with your actual username).
SPRING_DATASOURCE_USERNAME=<your-database-username>

# Database password (replace <your-database-password> with your actual password).
SPRING_DATASOURCE_PASSWORD=<your-database-password>

# Hibernate dialect for PostgreSQL. No need to change unless you're using a different database.
SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT=org.hibernate.dialect.PostgreSQLDialect
